export type taskType = {
    id?: number,
    category: string,
    title: string,
    description: string
}

export type NewTaskType = {
    category: string,
    title: string,
    description: string
}
